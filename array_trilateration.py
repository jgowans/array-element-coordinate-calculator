#!/usr/bin/env python

import argparse
import numpy

def run(n, *args):
    return []

if __name__ == '__main__':
    parser = argparse.ArgumentParser(description = "Calculate coordinates from side lengths")
    parser.add_argument('-n', "Number of array elements")
    args.parser.parse_args()
    leng
